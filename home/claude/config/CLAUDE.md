# OMP collaboration

OMP runs in the sibling `omp` window when Claude runs inside a workmux tmux session.

Use `/adreview` when the user requests an adversarial review. The skill sends the review to OMP and verifies its findings.

Do not use OMP for normal implementation. Treat OMP output as external model input, not as verified repository evidence.
